{
  "name": "quadcopter-rl",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "python3 -m http.server 3000",
    "build": "mkdir -p dist && echo 'Done' > dist/index.html",
    "clean": "rm -rf dist",
    "verify": "python3 scripts/verify_pretraining.py",
    "train": "python3 scripts/train.py --num-episodes 500"
  }
}
